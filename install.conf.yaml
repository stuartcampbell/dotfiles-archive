- clean: ['~', '~/.config']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig:  gitconfig
    ~/.i3: i3/
    ~/.screenrc screenrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
