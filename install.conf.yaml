- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.bin.Darwin: bin.Darwin/
    ~/.i3: i3/

- shell:
  - rm ~/bin.test
  - ln -s ~/.bin.`uname` ~/bin.test

- shell:
  - [git submodule update --init --recursive, Installing submodules]
